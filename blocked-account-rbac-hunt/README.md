# Blocked Account RBAC Hunt

Finds disabled users and generates a report containing role assignments and group memberships relating to those users.

## Usage

```pwsh
PS>. .\get-all.ps1
PS>start .\ignore\output.md
```